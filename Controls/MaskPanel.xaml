<UserControl x:Class="IceSky.WpfLoading.Sample.Controls.MaskPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IceSky.WpfLoading.Sample.Controls" xmlns:control="clr-namespace:IceSky.WpfLoading;assembly=IceSky.WpfLoading"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#F5F7FA" x:Name="ParentPanel">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid Width="200" Background="White" Margin="5">
            <StackPanel Margin="10" VerticalAlignment="Top">
                <TextBlock Text="遮罩层设置" FontSize="16" FontWeight="Bold" Margin="0 0 0 10"/>

                <StackPanel Orientation="Horizontal" Margin="0 10">
                    <TextBlock Text="模糊半径" FontSize="12" Foreground="#333"/>
                    <Slider x:Name="sldBlurRadius" Minimum="0" Maximum="30" Value="10" Width="80"/>
                    <TextBlock x:Name="txtBlurRadius" Text="{Binding ElementName=sldBlurRadius,Path=Value,StringFormat=0}" 
                                   VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0 10">
                    <TextBlock Text="不透明度" FontSize="12" Foreground="#333"/>
                    <Slider x:Name="sldMaskOpacity" Minimum="0" Maximum="1" Value="0.7" 
                                Width="80" TickFrequency="0.1" LargeChange="0.5"/>
                    <TextBlock x:Name="txtMaskOpacity" Text="{Binding ElementName=sldMaskOpacity,Path=Value,StringFormat=0.0}" 
                                   VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0 10">
                    <TextBlock Text="遮罩颜色" FontSize="12" Foreground="#333"/>
                    <TextBox x:Name="tbColorMsk" Width="80" Text="#FF4A90E2" Margin="10 0 5 0"></TextBox>
                    <Border x:Name="bdColorMsk" Width="24" Height="20" BorderThickness="1" BorderBrush="#333" Background="{Binding ElementName=tbColorMsk,Path=Text}"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0 10">
                    <TextBlock Text="超时时间(毫秒)" FontSize="12" Foreground="#333"/>
                    <TextBox x:Name="txtTimeoutMs" Text="8000" Margin="10 0">
                    </TextBox>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0 10">
                    <TextBlock Text="任务时长(秒)" FontSize="12" Foreground="#333"/>
                    <TextBox x:Name="txtTaskSeconds" Text="5" Margin="10 0"/>
                </StackPanel>
            </StackPanel>
        </Grid>

        <Grid Grid.Column="1">
            <StackPanel x:Name="BusinessGrid" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Image Source="https://picsum.photos/800/450" Width="500" Height="300"/>
                <StackPanel Orientation="Horizontal" Margin="0 5" HorizontalAlignment="Center">
                    <Button Content="Show Mask" Width="100" Height="40" Click="ShowMask_Click" Margin="5"/>
                    <Button Content="Task Test" Width="100" Height="40" Click="ExecuteTask_Click" Margin="5"/>
                </StackPanel>
                <TextBlock Text="业务内容区域" FontSize="20" Margin="0 20 0 0" HorizontalAlignment="Center"/>
            </StackPanel>

            <control:MaskPanel x:Name="maskPanel"
                                     TargetBlurElement="{Binding ElementName=BusinessGrid}"
                                     MaskColor="{Binding ElementName=tbColorMsk,Path=Text}"
                                     MaskOpacity="{Binding ElementName=sldMaskOpacity,Path=Value}"
                                     BlurRadius="{Binding ElementName=sldBlurRadius,Path=Value}"
                                     TimeoutMilliseconds="{Binding ElementName=txtTimeoutMs,Path=Text}"
                                     TaskCompleted="MaskPanel_TaskCompleted"
                                     TaskCancelled="MaskPanel_TaskCancelled"
                                     OnTimeout="MaskPanel_Timeout">
                <Border Width="300" Height="200" Background="White" CornerRadius="5">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="处理中..." FontSize="18" Margin="0 30 0 5"/>
                        <TextBlock x:Name="txtElapsedTime" Text="00:00:00" FontSize="16" Foreground="#E74C3C"/>
                        <TextBlock Name="tbMsg" Text="" FontSize="12" Foreground="#666" Margin="0 5"/>
                        <ProgressBar Width="200" Height="10" IsIndeterminate="True" Margin="0 10"/>
                        <Button Content="Cancel" Width="100" Height="30" Click="CancelTask_Click" Background="#E74C3C" Foreground="White" BorderThickness="0"/>
                    </StackPanel>
                </Border>
            </control:MaskPanel>
        </Grid>
    </Grid>
</UserControl>
